This directory contains scripts to automatically install the databrowser application.

From this directory, on the database server, you should simply be able to execute:

> sudo server_install

The scripts here will install Python packages pylons, sqlalchemy, scipy, matplotlib.